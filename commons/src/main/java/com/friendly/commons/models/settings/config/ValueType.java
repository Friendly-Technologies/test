package com.friendly.commons.models.settings.config;

/**
 * Enum that defines a Value Type of Interface
 *
 * @author Friendly Tech
 * @since 0.0.2
 */
public enum ValueType {
    STRING,
    INTEGER,
    BOOLEAN,
    LIST_INTEGER,
    LIST_STRING,
    SELECTOR,
    LIST_PARAMETER,
    LIST_MANUFACTURER
}
